name: MultiWorld
main: MultiWorld.MultiWorld
author: Fahrul8676
version: "1.14.0"
api: ["1.0.1"]
prefix: "§cMW§r"

commands:
  mw:
    description: "Perintah MultiWorld"

permissions:
  mw.command:
    description: "Perizinan MultiWorld"
    default: op
    children:
      mw.command.load:
        default: op
      mw.command.unload:
        default: op
      mw.command.create:
        default: op
      mw.command.delete:
        default: op
      mw.command.list:
        default: op
      mw.command.teleport:
        default: op
      mw.command.spawn:
        default: op
      mw.command.setspawn:
        default: op